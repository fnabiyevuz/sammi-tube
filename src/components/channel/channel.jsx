import { Button } from "@mui/material"
import { Link } from "react-router-dom"

const Channel = () => {

  return (
    <Link to={'/'}>
        <Button>Home</Button>
    </Link>
  )
}

export default Channel